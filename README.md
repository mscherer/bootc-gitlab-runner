Experimental bootc image 

This is meant to be used as a complement to https://gitlab.com/osci/ansible-role-gitlabci-runner.git

The image is here to be configured by ansible, as a experiment on combining bootc and ansible.
